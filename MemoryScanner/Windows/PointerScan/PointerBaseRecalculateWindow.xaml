<Window x:Class="MemoryScanner.Windows.PointerBaseRecalculateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Recalculate Pointer Base"
        Height="330"
        Width="700"
        MinHeight="300"
        MinWidth="620"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="CurrentBaseText"
                   Grid.Row="0"
                   Margin="0,0,0,10"
                   FontWeight="SemiBold"
                   Text="Current base: -"/>

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="230"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="180"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,0" Text="Difference"/>
            <TextBox x:Name="DifferenceTextBox"
                     Grid.Column="1"
                     Height="28"
                     VerticalContentAlignment="Center"
                     TextChanged="DifferenceTextBox_OnTextChanged"/>

            <TextBlock Grid.Column="3" VerticalAlignment="Center" Margin="0,0,8,0" Text="Direction"/>
            <ComboBox x:Name="DirectionBox"
                      Grid.Column="4"
                      Height="28"
                      SelectionChanged="DirectionBox_OnSelectionChanged"/>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Margin="0,0,8,0" VerticalAlignment="Center" Text="Recalculated Base"/>
            <TextBlock x:Name="RecalculatedBaseText" Grid.Column="1" VerticalAlignment="Center" Text="-"/>
        </Grid>

        <Grid Grid.Row="3" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Margin="0,0,8,0" VerticalAlignment="Center" Text="Resolved Address"/>
            <TextBlock x:Name="ResolvedAddressText" Grid.Column="1" VerticalAlignment="Center" Text="-"/>
        </Grid>

        <Grid Grid.Row="4" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Margin="0,0,8,0" VerticalAlignment="Center" Text="Value Preview"/>
            <TextBlock x:Name="ValuePreviewText" Grid.Column="1" VerticalAlignment="Center" Text="-"/>
        </Grid>

        <Grid Grid.Row="5" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="StatusText"
                       Grid.Column="0"
                       VerticalAlignment="Center"
                       Foreground="#FF333333"
                       Text="Enter a difference to preview."/>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Width="100" Height="30" Margin="0,0,8,0" Click="Cancel_OnClick"/>
                <Button x:Name="ApplyButton" Content="Apply" Width="110" Height="30" Click="Apply_OnClick"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
