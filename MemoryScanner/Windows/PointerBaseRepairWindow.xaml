<Window x:Class="MemoryScanner.Windows.PointerBaseRepairWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Repair Pointer Base"
        Height="640"
        Width="980"
        MinHeight="520"
        MinWidth="860"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="CurrentBaseText" Grid.Row="0" Margin="0,0,0,8" FontWeight="SemiBold" Text="Current base: -"/>

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="170"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="130"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,0" Text="Data Type"/>
            <ComboBox x:Name="DataTypeBox" Grid.Column="1" Height="28"/>

            <TextBlock Grid.Column="3" VerticalAlignment="Center" Margin="0,0,8,0" Text="Expected Value"/>
            <TextBox x:Name="ExpectedValueText" Grid.Column="4" Height="28" VerticalContentAlignment="Center"/>

            <TextBlock Grid.Column="6" VerticalAlignment="Center" Margin="0,0,8,0" Text="Range (+/- bytes)"/>
            <TextBox x:Name="RangeText" Grid.Column="7" Height="28" VerticalContentAlignment="Center" Text="0x400000"/>

            <TextBlock Grid.Column="9" VerticalAlignment="Center" Margin="0,0,8,0" Text="Step (bytes)"/>
            <TextBox x:Name="StepText" Grid.Column="10" Height="28" VerticalContentAlignment="Center" Text="4"/>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,0" Text="Max Results"/>
            <TextBox x:Name="MaxResultsText" Grid.Column="1" Height="28" VerticalContentAlignment="Center" Text="200"/>

            <TextBlock Grid.Column="3" VerticalAlignment="Center" Margin="0,0,8,0" Text="Offsets"/>
            <TextBlock x:Name="OffsetsText" Grid.Column="4" VerticalAlignment="Center" Text="-" TextTrimming="CharacterEllipsis"/>

            <Button x:Name="StartScanButton" Grid.Column="6" Width="110" Height="30" Margin="0,0,8,0" Content="Scan" Click="StartScan_OnClick"/>
            <Button x:Name="CancelScanButton" Grid.Column="7" Width="100" Height="30" Content="Cancel" Click="CancelScan_OnClick" IsEnabled="False"/>
        </Grid>

        <DataGrid x:Name="ResultsGrid"
                  Grid.Row="3"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  ScrollViewer.CanContentScroll="True"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Base Address" Binding="{Binding BaseAddressText}" Width="2*"/>
                <DataGridTextColumn Header="Resolved Address" Binding="{Binding ResolvedAddressText}" Width="2*"/>
                <DataGridTextColumn Header="Value" Binding="{Binding ValueText}" Width="1.4*"/>
                <DataGridTextColumn Header="Pointer Size" Binding="{Binding PointerSizeLabel}" Width="*"/>
                <DataGridTextColumn Header="Base (Hex)" Binding="{Binding BaseHex}" Width="1.4*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="4" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <ProgressBar x:Name="ScanProgressBar" Width="260" Height="18" Minimum="0" Maximum="100" Value="0"/>
                <TextBlock x:Name="StatusText" Margin="8,0,0,0" VerticalAlignment="Center" Text="Idle"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Apply Selected" Width="130" Height="30" Margin="0,0,8,0" Click="ApplySelected_OnClick"/>
                <Button Content="Close" Width="100" Height="30" Click="Close_OnClick"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
